{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.ilia-sokolov/officeagent",
  "title": "OfficeAgent.NET",
  "description": "Create and edit Word, PowerPoint, and Excel files with typed plans and safe previews",
  "repository": {
    "url": "https://github.com/ilia-sokolov/OfficeAgent.NET",
    "source": "github"
  },
  "version": "1.0.0",
  "packages": [
    {
      "registryType": "nuget",
      "identifier": "OfficeAgent.Mcp",
      "version": "1.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "OfficeAgent__FileSystemConnections__0__ConnectionId",
          "description": "Connection id the agent uses to address documents on the filesystem (any non-empty string). Optional: with nothing configured the server starts with an in-memory session connection whose documents last only while it runs. Set this and RootPath to edit documents on disk instead; SharePoint is OfficeAgent__SharePointConnections__0__*",
          "isRequired": false,
          "default": "documents",
          "placeholder": "documents"
        },
        {
          "name": "OfficeAgent__FileSystemConnections__0__RootPath",
          "description": "Absolute path to the directory this filesystem connection may read and write documents under. Only .docx is allowed unless OfficeAgent__FileSystemConnections__0__AllowedExtensions__N adds more (.pptx for decks or .xlsx for workbooks). Deck connections usually want OfficeAgent__FileSystemConnections__0__DefaultChangeMode=Direct because PowerPoint cannot record tracked changes",
          "format": "filepath",
          "isRequired": false,
          "placeholder": "/path/to/documents"
        },
        {
          "name": "OfficeAgent__AllowCreation",
          "description": "Set to true to expose create_document, so an agent can author a new file in the connection rather than only editing existing ones. Off by default: creating agent-named files under a connection root is a capability the host should choose deliberately",
          "isRequired": false,
          "default": "false",
          "placeholder": "true"
        }
      ]
    }
  ]
}
